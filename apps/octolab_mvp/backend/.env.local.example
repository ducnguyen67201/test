# backend/.env.local.example
#
# Local development overrides - this file is gitignored.
# Copy to .env.local and customize:
#   cp backend/.env.local.example backend/.env.local
#   chmod 600 backend/.env.local
#
# Values here override backend/.env
# SECURITY: This file contains secrets - do not commit!

# =============================================================================
# Apache Guacamole Configuration
# =============================================================================

# Enable Guacamole integration
GUAC_ENABLED=true

# Guacamole web UI base URL (localhost only in dev)
GUAC_BASE_URL=http://127.0.0.1:8081/guacamole

# Guacamole admin credentials for API access
# CHANGE THESE from defaults after first setup!
GUAC_ADMIN_USER=guacadmin
GUAC_ADMIN_PASSWORD=guacadmin

# Fernet encryption key for per-lab Guacamole passwords
# This key MUST remain stable - regenerating it will break existing labs!
# Auto-generated by: make dev-up (via ensure_env_local.py)
# Manual generation: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# GUAC_ENC_KEY=<auto-generated-do-not-change>

# guacd container name (for network connect/disconnect)
GUACD_CONTAINER_NAME=octolab-guacd

# =============================================================================
# Development Overrides
# =============================================================================

# Enable self-service user registration (dev only; default false in prod)
# SECURITY: Only enable in dev/test environments
ALLOW_SELF_SIGNUP=true

# Override log level for local development
# LOG_LEVEL=DEBUG

# Override JWT secret for local dev (use a real secret in production!)
# SECRET_KEY=dev-only-secret-change-in-production

# =============================================================================
# Notes
# =============================================================================
#
# Why .env.local?
# - .env is committed and contains non-secret defaults
# - .env.local is gitignored and contains your local secrets
# - This allows sharing common config while keeping secrets local
#
# Important:
# - GUAC_ENC_KEY must remain stable once set (used to encrypt/decrypt passwords)
# - File permissions should be 600 (readable only by owner)
# - Never commit this file or share it
